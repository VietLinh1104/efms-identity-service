version: '3.8'

services:
  identity-service:
    build: .
    container_name: efms-identity-service
    ports:
      - "${PORT:-8081}:${PORT:-8081}"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-prod}
      - DB_URL=${DB_URL:-jdbc:postgresql://identity-db:5432/efms_db}
      - DB_USERNAME=${DB_USERNAME:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-yourpassword}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRATION_MS=${JWT_EXPIRATION_MS:-86400000}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - RESEND_FROM_EMAIL=${RESEND_FROM_EMAIL:-onboarding@resend.dev}
      - PUBLIC_EFMS_APP_FRONTEND_URL=${PUBLIC_EFMS_APP_FRONTEND_URL:-http://localhost:5173/login}
    depends_on:
      - identity-db

  identity-db:
    image: postgres:16-alpine
    container_name: efms-db
    environment:
      - POSTGRES_DB=efms_db
      - POSTGRES_USER=${DB_USERNAME:-efms}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-Ltx622004$01}
    ports:
      - "5433:5432" # Changed to 5433 to avoid conflict with local postgres if any
    volumes:
      - identity-db-data:/var/lib/postgresql/data

volumes:
  identity-db-data:
